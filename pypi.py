'''Importing OS library into .py file
OS is built in Python library
OS allows us to access operating system, as well as math, text and file handling modules'''
import os

'''Import Flask
Flask is a Python framework and is external
We need to us pip3 in CLI to install it first before is can be used
Type sudo pip3 install Flask'''
from flask import Flask